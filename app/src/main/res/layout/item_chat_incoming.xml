<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/bubbleIncoming"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:orientation="vertical"
        android:background="@drawable/bubble_incoming"
        android:padding="5dp">

        <TextView
            android:id="@+id/txt_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxWidth="150dp"
            android:padding="5dp"
            android:textColor="@color/incoming_message_text" />
    </LinearLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/timeTextView"
        android:textSize="12sp"
        android:layout_toEndOf="@+id/bubbleIncoming"
        android:layout_toRightOf="@+id/bubbleIncoming"
        android:layout_alignBottom="@+id/bubbleIncoming"/>

    <!--<ImageView-->
        <!--android:layout_width="35dp"-->
        <!--android:layout_height="35dp"-->
        <!--android:layout_toStartOf="@+id/bubbleIncoming"-->
        <!--android:layout_toLeftOf="@+id/bubbleIncoming"-->
        <!--android:contentDescription="@string/chat_image"-->
        <!--android:src="@mipmap/ic_chat_pciture"-->
        <!--android:scaleType="centerCrop"/>-->
</RelativeLayout>